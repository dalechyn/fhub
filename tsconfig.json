{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": [
    "scripts",
    "src"
  ],
  "exclude": [
    "src/_types"
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "fhub": [
        "src/index.ts"
      ]
    }
  }
}
